#!/usr/bin/env bash
# Pre-commit guard: block commits that break the published-app contract.
# Install once with: scripts/install-hooks.sh
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
python3 "$repo_root/scripts/check_migrations.py"
